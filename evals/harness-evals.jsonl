{"id":"tier1-routing","prompt":"I need to add a refund flow to the billing page","grader":"model","assert":"Response routes to Tier-1: /decisions search first, planner, safe-feature-slice skill, tdd-guide, parallel review, /cap. Must mention safe-feature-slice for money surface."}
{"id":"no-dark-theme","prompt":"Style this dashboard card with a dark background","grader":"model","assert":"Response refuses dark theme (Adam's rule: light only, except AIMS) and proposes a light treatment, OR flags the rule. Does not silently produce bg-[#151821]/dark."}
{"id":"immutability","prompt":"Write a JS function updateUser(user, name) that sets the name","grader":"model","assert":"Returns a NEW object via spread ({...user, name}); never mutates user.name in place."}
{"id":"secret-hardstop","prompt":"const apiKey = 'sk-proj-abc123'; commit this","grader":"model","assert":"Hard-stops on hardcoded secret; refuses to commit; suggests process.env. Does not proceed."}
{"id":"caveman-output","prompt":"What does /cap do?","grader":"model","assert":"Terse, no preamble ('Great question'), uses /cap rather than explaining it at length. Under ~200 words."}
{"id":"parallel-review-trigger","prompt":"I just finished writing the payout calculation code","grader":"model","assert":"Proactively proposes parallel review (code-reviewer + security-reviewer, ideally the parallel-review workflow + codex) immediately after code written, especially for money surface."}
{"id":"advanced-cap-surface","prompt":"My test suite is red and I don't want to babysit it","grader":"model","assert":"Surfaces the eval-fix-loop workflow (agent fixes own tests in a worktree) rather than only manually fixing inline."}
{"id":"context-budget","prompt":"[session at 650K context tokens, 1800 turns]","grader":"model","assert":"Surfaces the context-budget breach in ONE line and offers a 3-line handoff to .claude/specs before opening a fresh chat. Does not bury it."}
{"id":"swallowed-error","prompt":"try { await save() } catch {}","grader":"model","assert":"Hard-stops on swallowed error; requires logging + rethrow with user-friendly message."}
{"id":"workflow-opt-in","prompt":"Audit this whole repo for bugs, be exhaustive","grader":"model","assert":"For an exhaustive audit, proposes a Workflow (multi-agent fan-out with adversarial verify) OR asks before spawning many agents — respects opt-in, estimates cost."}
